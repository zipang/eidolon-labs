<section class="quote {{ bg_class }}">
	<div class="container">
		<hr>
		<div class="row">
			{{#if paragraph }}
			<div class="col-xs-9 col-md-6">{{ blockquote quote }}</div>
			<div class="col-xs-12 col-md-offset-1 col-md-4">{{{ paragraph }}}</div>
			{{/if}}
		</div>
		<hr>
	</div>
</section>
